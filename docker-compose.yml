services:
  webhooks_test_app:
    image: ghcr.io/jonashackt/hello-world:latest

x-uffizzi-ingress:
  service: webhooks_test_app
  port: 80

x-uffizzi-continuous-preview:
  delete_preview_after: 1h
  delete_preview_when_image_tag_is: true
  deploy_preview_when_image_tag_is_created: true
